{% extends 'NaonedDatabaseConfigBundle::layout.html.twig' %}
{% form_theme form 'NaonedDatabaseConfigBundle::form_theme.html.twig' %}

{% block content %}

    {{ form_start(form, { attr: {'novalidate': 'novalidate'} }) }}

        {{ form_widget(form) }}

        {% block buttons %}
            <button type="submit">{% trans %}Save{% endtrans %}</button>
        {% endblock %}

    {{ form_end(form) }}

{% endblock %}
